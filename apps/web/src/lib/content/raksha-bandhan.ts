/** Soft stub — legacy festival content removed from BlossomPot gift storefront. */
export const rakshaBandhanFaqs: { q: string; a: string }[] = [];

export const rakshaBandhanContent = {
  headline: "Celebration gifts with BlossomPot",
  intro: [] as string[],
  sections: [] as { heading: string; paragraphs: string[] }[],
};
